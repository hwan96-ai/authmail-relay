# Project Context

## When to use this

이 저장소에서 작업을 시작할 때 먼저 읽는다. 특히 요청이 Python 라이브러리, FastAPI HTTP 서비스, 문서 중 어디에 영향을 주는지 판단하기 전에 사용한다.

## 프로젝트 요약

`email-service` 는 SMTP 기반 HTML 이메일 발송 패키지이며 두 가지 모드를 지원한다:

- Python 라이브러리 모드.
- FastAPI HTTP 서비스 모드.

공개 사용법은 [README.md](../../README.md)를 참조한다.
